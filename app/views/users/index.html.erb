<h2><%= t('views.user.index.user_list') %></h2>
<table>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= link_to t('views.common.show'), user %></td>
    </tr>
  <% end %>
</table>

<%= paginate @users %>
